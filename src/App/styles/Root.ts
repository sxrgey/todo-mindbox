import { Box, styled } from '@mui/material';

const Root = styled(Box)`
  position: relative;
  display: flex;
  height: 100vh;
  width: 100vw;
  justify-content: center;
  align-items: center;

  z-index: 0;
`;

export default Root;
