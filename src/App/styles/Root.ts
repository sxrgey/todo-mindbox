import { Box, styled } from '@mui/material';

const Root = styled(Box)`
  display: flex;
  flex-direction: column;
  height: 100vh;
  width: 100vw;
  /* justify-content: center; */
  align-items: center;
`;

export default Root;
