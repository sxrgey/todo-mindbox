import { styled, Typography } from '@mui/material';

const Label = styled(Typography)`
  text-align: center;
  font-size: 6rem;
  font-weight: 100;
  color: rgb(235, 217, 216);
`;

export default Label;
